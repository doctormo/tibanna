region: us-east-1

function_name: create_beanstalk 
handler: service.handler
role: sysadmin 
description: creates a beanstalk instance on aws, optionally deleting the original 

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
timeout: 300 
memory_size: 256 
